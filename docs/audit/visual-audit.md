# WinUI 3 visual audit report

> Generated by `scripts/audit/audit-visual.ps1`. Every row below carries the evidence that produced it.
> `unverifiable` means the criterion could not be decided with the available evidence and is **not** a pass.

## Result

| status | count |
|---|---|
| pass | 27 |
| fail | 1 |
| unverifiable | 17 |
| total | 45 |

## Subject

| field | value |
|---|---|
| executable | `F:\WhalesLauncher\.probe\winui3-smoke\bin\Debug\net10.0-windows10.0.26100.0\win-x64\WhalesLauncher.Smoke.exe` |
| exe SHA256 | `3554d4bec5c0d70bd534617b61b51f77063c7fb02dd22d1fe3e850b9f68a6713` |
| exe version | 1.0.0.0 |
| exe built | 2026-09-21T13:33:52.9607694+08:00 |
| git | winui3-rewrite @ a239e77a28e4f07a66abda0287c14cc27d657e96 |
| working tree dirty | True |
| spec SHA256 | `7b4651ba892da4557cc7625c5672ec282049d259feb6e357ce9835c1a088e78a` |
| themes | Light, Dark |
| started | 2026-09-21 13:53:58 |
| screen DPI | 96 |
| system app theme at start | Light |
| system theme changed by tool | True |
| text scale factor | 100% |
| high contrast active | False |

## Criteria

| id | scope | status | criterion | measurement | note |
|---|---|---|---|---|---|
| C5 | page | pass | render stability between equivalent frames | see visual-audit.json | - |
| C5 | page | pass | render stability between equivalent frames | see visual-audit.json | - |
| C5 | page | pass | render stability between equivalent frames | see visual-audit.json | - |
| C5 | page | pass | render stability between equivalent frames | see visual-audit.json | - |
| C1 | run | pass | window capture succeeded for every page | see visual-audit.json | - |
| C2 | page | pass | capture contains real rendered content (smoke-baseline.light.png) | see visual-audit.json | - |
| C2 | page | pass | capture contains real rendered content (smoke-expander.light.png) | see visual-audit.json | - |
| C2 | page | pass | capture contains real rendered content (smoke-baseline.dark.png) | see visual-audit.json | - |
| C2 | page | pass | capture contains real rendered content (smoke-expander.dark.png) | see visual-audit.json | - |
| C3 | page | pass | UI Automation exposes the rendered element tree (smoke-baseline.light.png) | see visual-audit.json | - |
| C3 | page | pass | UI Automation exposes the rendered element tree (smoke-expander.light.png) | see visual-audit.json | - |
| C3 | page | pass | UI Automation exposes the rendered element tree (smoke-baseline.dark.png) | see visual-audit.json | - |
| C3 | page | pass | UI Automation exposes the rendered element tree (smoke-expander.dark.png) | see visual-audit.json | - |
| C4 | page | pass | no text element overflows its container (smoke-baseline.light.png) | see visual-audit.json | geometric overflow only; whether an ellipsis is actually painted for trimmed text still needs the image |
| C4 | page | pass | no text element overflows its container (smoke-expander.light.png) | see visual-audit.json | geometric overflow only; whether an ellipsis is actually painted for trimmed text still needs the image |
| C4 | page | pass | no text element overflows its container (smoke-baseline.dark.png) | see visual-audit.json | geometric overflow only; whether an ellipsis is actually painted for trimmed text still needs the image |
| C4 | page | pass | no text element overflows its container (smoke-expander.dark.png) | see visual-audit.json | geometric overflow only; whether an ellipsis is actually painted for trimmed text still needs the image |
| C7 | page | fail | keyboard focus changes the rendering | see visual-audit.json | the change covers only 0.00108 of the focused element's area: too small to be a focus indicator |
| C7 | page | pass | keyboard focus changes the rendering | see visual-audit.json | focus moved and the changed pixels lie inside the focused element; whether the indicator is the system focus rect still needs a human look at the image |
| C7 | page | unverifiable | keyboard focus changes the rendering | see visual-audit.json | the changed pixels are outside the focused element's rectangle (a text caret or an unrelated repaint is the likely cause), so they prove nothing about the focus indicator |
| C7 | page | pass | keyboard focus changes the rendering | see visual-audit.json | focus moved and the changed pixels lie inside the focused element; whether the indicator is the system focus rect still needs a human look at the image |
| C8 | page | unverifiable | disabled controls exist and are discoverable (smoke-baseline.light.png) | see visual-audit.json | this page state contains no disabled control, so the disabled visual cannot be assessed from it |
| C8 | page | unverifiable | disabled controls exist and are discoverable (smoke-expander.light.png) | see visual-audit.json | this page state contains no disabled control, so the disabled visual cannot be assessed from it |
| C8 | page | unverifiable | disabled controls exist and are discoverable (smoke-baseline.dark.png) | see visual-audit.json | this page state contains no disabled control, so the disabled visual cannot be assessed from it |
| C8 | page | unverifiable | disabled controls exist and are discoverable (smoke-expander.dark.png) | see visual-audit.json | this page state contains no disabled control, so the disabled visual cannot be assessed from it |
| C14 | run | pass | Light and Dark produce visibly different renderings | see visual-audit.json | - |
| C9 | page | unverifiable | measured content margins are multiples of 4 epx (smoke-baseline.light.png) | see visual-audit.json | edge detection alone cannot decide the 4 epx grid: the first painted pixel is an anti-aliased glyph or a full-bleed background, not the layout box. Needs XAML static analysis or an instrumented layout dump. |
| C9 | page | unverifiable | measured content margins are multiples of 4 epx (smoke-expander.light.png) | see visual-audit.json | edge detection alone cannot decide the 4 epx grid: the first painted pixel is an anti-aliased glyph or a full-bleed background, not the layout box. Needs XAML static analysis or an instrumented layout dump. |
| C9 | page | unverifiable | measured content margins are multiples of 4 epx (smoke-baseline.dark.png) | see visual-audit.json | edge detection alone cannot decide the 4 epx grid: the first painted pixel is an anti-aliased glyph or a full-bleed background, not the layout box. Needs XAML static analysis or an instrumented layout dump. |
| C9 | page | unverifiable | measured content margins are multiples of 4 epx (smoke-expander.dark.png) | see visual-audit.json | edge detection alone cannot decide the 4 epx grid: the first painted pixel is an anti-aliased glyph or a full-bleed background, not the layout box. Needs XAML static analysis or an instrumented layout dump. |
| C6 | page | unverifiable | font sizes come from the built-in type ramp (smoke-baseline.light.png) | see visual-audit.json | the managed UI Automation API in-package exposes no reliable font-size attribute, and a screenshot cannot be reduced to numeric font sizes without an OCR step. Needs XAML static analysis or an explicitly instrumented automation property. |
| C6 | page | unverifiable | font sizes come from the built-in type ramp (smoke-expander.light.png) | see visual-audit.json | the managed UI Automation API in-package exposes no reliable font-size attribute, and a screenshot cannot be reduced to numeric font sizes without an OCR step. Needs XAML static analysis or an explicitly instrumented automation property. |
| C6 | page | unverifiable | font sizes come from the built-in type ramp (smoke-baseline.dark.png) | see visual-audit.json | the managed UI Automation API in-package exposes no reliable font-size attribute, and a screenshot cannot be reduced to numeric font sizes without an OCR step. Needs XAML static analysis or an explicitly instrumented automation property. |
| C6 | page | unverifiable | font sizes come from the built-in type ramp (smoke-expander.dark.png) | see visual-audit.json | the managed UI Automation API in-package exposes no reliable font-size attribute, and a screenshot cannot be reduced to numeric font sizes without an OCR step. Needs XAML static analysis or an explicitly instrumented automation property. |
| C13 | run | unverifiable | status is not conveyed by colour alone | see visual-audit.json | deciding this from pixels plus UIA names would require a per-state mapping that the tool does not have; a human must compare the state samples |
| C10 | run | unverifiable | body text contrast is at least 4.5:1 | see visual-audit.json | no text-to-pixel binding is available, so per-span contrast cannot be computed; a text-cluster contrast approximation is deliberately NOT used because it would produce a false pass |
| C11 | run | unverifiable | the four built-in contrast themes were exercised | see visual-audit.json | this tool does not switch contrast themes (that is a system accessibility setting) and no per-theme comparison is implemented, so the claim cannot be evidenced here |
| C12 | run | unverifiable | hover / open-overlay states were captured | see visual-audit.json | the configured page list declares no hover/click step, so no hover or overlay evidence exists. Concluding anything about those states would be fabrication. |
| C15 | run | pass | the audit is tied to the exact spec revision | see visual-audit.json | - |
| C16 | run | pass | every PNG matches its window dimensions | see visual-audit.json | - |
| C17 | run | pass | each captured state is a distinct frame | see visual-audit.json | - |
| C18 | run | pass | no orphaned application process is left behind | see visual-audit.json | - |
| C19 | run | pass | the report is traceable to the exact artefact | see visual-audit.json | - |
| C20 | run | pass | the report itself is complete | see visual-audit.json | - |
| C21 | run | pass | every non-pass criterion states a reason | see visual-audit.json | - |

## Screenshots

| page | theme | state | png | distinct colours | non-background | mean luma | sha256 (16) |
|---|---|---|---|---|---|---|---|
| smoke-baseline | Light | base | smoke-baseline.light.png | 97 | 0.130536 | 243.21 | 0e96e55e656ef66a |
| smoke-baseline | Light | focus | smoke-baseline.light.focus.png | 95 | 0.130517 | 243.12 | 1958e81a763581e4 |
| smoke-baseline | Light | stable | smoke-baseline.light.stable.png | 93 | 0.130852 | 243.06 | 01445dea7745a060 |
| smoke-expander | Light | base | smoke-expander.light.png | 97 | 0.132852 | 242.59 | 5461bcbb36e51f7d |
| smoke-expander | Light | focus | smoke-expander.light.focus.png | 94 | 0.13122 | 242.99 | c398455466066c79 |
| smoke-expander | Light | stable | smoke-expander.light.stable.png | 95 | 0.132842 | 242.59 | 7bdcc8477de1789e |
| smoke-baseline | Dark | base | smoke-baseline.dark.png | 91 | 0.999981 | 17.01 | 0d0f09da9f520316 |
| smoke-baseline | Dark | focus | smoke-baseline.dark.focus.png | 86 | 0.999981 | 17 | 17e8dcab03c25dbe |
| smoke-baseline | Dark | stable | smoke-baseline.dark.stable.png | 88 | 0.999981 | 17.07 | b4ef98d670cf53e3 |
| smoke-expander | Dark | base | smoke-expander.dark.png | 92 | 0.994086 | 17.6 | 10e19627ab204386 |
| smoke-expander | Dark | focus | smoke-expander.dark.focus.png | 90 | 0.999981 | 17.14 | 7693a73758184e42 |
| smoke-expander | Dark | stable | smoke-expander.dark.stable.png | 93 | 0.994086 | 17.59 | ab95ee87d800bb5b |

## Unverifiable criteria and why

- **C7 keyboard focus changes the rendering** - the changed pixels are outside the focused element's rectangle (a text caret or an unrelated repaint is the likely cause), so they prove nothing about the focus indicator
- **C8 disabled controls exist and are discoverable** - this page state contains no disabled control, so the disabled visual cannot be assessed from it
- **C8 disabled controls exist and are discoverable** - this page state contains no disabled control, so the disabled visual cannot be assessed from it
- **C8 disabled controls exist and are discoverable** - this page state contains no disabled control, so the disabled visual cannot be assessed from it
- **C8 disabled controls exist and are discoverable** - this page state contains no disabled control, so the disabled visual cannot be assessed from it
- **C9 measured content margins are multiples of 4 epx** - edge detection alone cannot decide the 4 epx grid: the first painted pixel is an anti-aliased glyph or a full-bleed background, not the layout box. Needs XAML static analysis or an instrumented layout dump.
- **C9 measured content margins are multiples of 4 epx** - edge detection alone cannot decide the 4 epx grid: the first painted pixel is an anti-aliased glyph or a full-bleed background, not the layout box. Needs XAML static analysis or an instrumented layout dump.
- **C9 measured content margins are multiples of 4 epx** - edge detection alone cannot decide the 4 epx grid: the first painted pixel is an anti-aliased glyph or a full-bleed background, not the layout box. Needs XAML static analysis or an instrumented layout dump.
- **C9 measured content margins are multiples of 4 epx** - edge detection alone cannot decide the 4 epx grid: the first painted pixel is an anti-aliased glyph or a full-bleed background, not the layout box. Needs XAML static analysis or an instrumented layout dump.
- **C6 font sizes come from the built-in type ramp** - the managed UI Automation API in-package exposes no reliable font-size attribute, and a screenshot cannot be reduced to numeric font sizes without an OCR step. Needs XAML static analysis or an explicitly instrumented automation property.
- **C6 font sizes come from the built-in type ramp** - the managed UI Automation API in-package exposes no reliable font-size attribute, and a screenshot cannot be reduced to numeric font sizes without an OCR step. Needs XAML static analysis or an explicitly instrumented automation property.
- **C6 font sizes come from the built-in type ramp** - the managed UI Automation API in-package exposes no reliable font-size attribute, and a screenshot cannot be reduced to numeric font sizes without an OCR step. Needs XAML static analysis or an explicitly instrumented automation property.
- **C6 font sizes come from the built-in type ramp** - the managed UI Automation API in-package exposes no reliable font-size attribute, and a screenshot cannot be reduced to numeric font sizes without an OCR step. Needs XAML static analysis or an explicitly instrumented automation property.
- **C13 status is not conveyed by colour alone** - deciding this from pixels plus UIA names would require a per-state mapping that the tool does not have; a human must compare the state samples
- **C10 body text contrast is at least 4.5:1** - no text-to-pixel binding is available, so per-span contrast cannot be computed; a text-cluster contrast approximation is deliberately NOT used because it would produce a false pass
- **C11 the four built-in contrast themes were exercised** - this tool does not switch contrast themes (that is a system accessibility setting) and no per-theme comparison is implemented, so the claim cannot be evidenced here
- **C12 hover / open-overlay states were captured** - the configured page list declares no hover/click step, so no hover or overlay evidence exists. Concluding anything about those states would be fabrication.

## Raw evidence

- Machine readable report: `F:\WhalesLauncher\docs\audit\visual-audit.json`
- Per page capture dumps: `.probe/audit/capture-<page>-<theme>.json`
- Per page step files: `.probe/audit/steps-<page>-<theme>.json`

